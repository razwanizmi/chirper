<%= form_with(model: chirp) do |f| %>
  <%= f.label(:content, "🐦 Share your thoughts with the world") %>
  <%= f.text_area(:content, placeholder: "I feel...", required: true) %>
  <%= f.submit("Chirp") %>
<% end %>
