<%= turbo_stream.update(
      "new_chirp",
      partial: "new_form",
      locals: { chirp: @new_chirp },
    ) %>

<%= turbo_stream.prepend(
      "chirps",
      partial: "chirp",
      locals: { chirp: @chirp },
    ) %>

<%= render_flash_stream %>
