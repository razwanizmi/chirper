<div class="container flashes">
  <% flash.each do |type, message| %>
    <%= content_tag(
          :div,
          class: "flashes__flash flashes__flash--#{type}",
          data: { controller: "flash" },
        ) do %>
      <%= content_tag(:small, message) %>
    <% end %>
  <% end %>
</div>
